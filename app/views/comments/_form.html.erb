<%= form_fot([@note, @note.comment.build]) do |f| %>
	<p>
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	</p>

	<p>
		<%= f.label :content %><br>
		<%= f.text_area :content %>
	</p>
	</br>
	<p>
		<%= f.submit %>
	</p>

	<% end %>