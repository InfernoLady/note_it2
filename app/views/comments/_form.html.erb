<div class="commentbox">
	<%= simple_form_for([@note, @note.comments.build]) do |f| %>
	<%= f.input :content, input_html: { class: 'commentcont' }  %>
	<%= f.submit :class => 'btn btn-success btn-sm' %>
	<% end %>

</div>