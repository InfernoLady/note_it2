<div class="comment clearfix">
	<div class="comment_content">
		<p class="comment_content"><%= "#{comment.user.email}: #{comment.content}" %><p>
	</div>

	<p><%= link_to 'Delete', [comment.note, comment], method: :delete, data: { confirm: "Are you sure?" } %></p>

</div>