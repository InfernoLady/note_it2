<div class="comment clearfix">
	<div class="comment_content">
		<p class="comment_content"><%= "#{comment.content}" %><p> <!--"#{comment.user.email}:-->
	</div>

	<p><%= link_to 'Remove comment', [comment.note, comment], method: :delete, data: { confirm: "Are you sure?" }, :class => 'btn btn-danger btn-xs' %></p>
</div>

