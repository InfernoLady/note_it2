var stateObj = { foo: "bar" };
history.pushState( stateObj, "new page title", "/notes?date=<%= @date %>" );
$("#notes").html("<%= escape_javascript(render partial: 'calendar', locals: {current_date: @date, notes_by_date: @notes_by_date}) %>").hide().fadeIn('fast');